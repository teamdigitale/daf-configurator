package controllers;

import it.gov.daf.common.authentication.Authentication;
import org.pac4j.play.store.PlaySessionStore;
import play.api.Configuration;
import javax.inject.*;
import play.mvc.*;

public class ApiDocController extends Controller {
    Authentication authentication;

    @Inject
    private ApiDocController(Configuration configuration, PlaySessionStore playSessionStore) {
        authentication.apply(configuration, playSessionStore);
    }

    public Result api() {
        return redirect("/assets/lib/swagger-ui/index.html?/url=/assets/swagger.json");
    }
}